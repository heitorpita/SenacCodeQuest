let numero = 1;

while (numero <= 20) {
    if (numero % 2 === 0) {
        console.log(`${numero} é par`);
    } else {
        console.log(`${numero} é impar`);
        
    }
    numero++;
}








// 5. Contagem de números pares até 20
// Use `while` para exibir apenas os números pares de 1 a 20.
// Dica: Use `if` para verificar se o número é par antes de exibir.