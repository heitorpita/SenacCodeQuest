const nome = "Lucas";

const sobrenome = "Lima";

let salario_por_hora = 7.5;

let horas_trabalhadas = 25 * 4;

let salario_final = salario_por_hora * horas_trabalhadas;

console.log(
    `o salario de ${nome} ${sobrenome} é de R$ ${salario_final.toFixed()}`
);




// um trabalhador ganha por hora, guarde o valor da hora trabalhada e o numeros de hoas e depois calcule o salario final