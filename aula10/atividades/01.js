let camisa = ["camisa preta", "camisa azul", "camisa rosa"]
let calca = ["calca preta", "calca roxa" , "calca verde-limao"]


for (i = 0; i < camisa.length; i++) {
    for (j = 0; j < camisa.length)
    
}